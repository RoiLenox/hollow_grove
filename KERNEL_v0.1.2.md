# Kernel v0.1.2

Date: 2026-07-06

Root recursion:

`Point -> Triway -> HollowGrove -> Bond + Atmosphere -> CurrentSeam -> AuraBeam -> Point`

## Boundary Lock

- `KernelPass` represents one completed recursion only.
- The TUI witnesses one completed pass only.
- There is no history.
- There is no replay.
- There is no continuing state.
- There is no Driver yet.

## Deferral

Continuing recursion is deferred until the kernel naturally demands recurrence beyond one pass.

## The Runtime Federation boundary

The canonical Hollow Grove name for continuity above completed kernel passes
is **The Runtime Federation**.

The Runtime Federation does not remove this kernel's boundary lock. The
universal kernel still computes one bounded pass and contains no House,
Central Junction, Function, route, gameplay, archive-federation, or
presentation law. A completed pass may be cited as immutable evidence by the
constitutional runtime, but no later layer may reopen or rewrite it.

The repeating Hollow Grove grammar:

`The Way Back -> The Initiation -> The Gathering -> The Festival -> The Way Back`

is kernel-facing constitutional language implemented above this pure kernel.
It carries confirmed results between passes without adding continuing world
state to the universal recursion primitive itself.

The authoritative canon for that boundary is
`THE_RUNTIME_FEDERATION_V1.md`.

## Purpose

This note protects the architecture from premature expansion.
